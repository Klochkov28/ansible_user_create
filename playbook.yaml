#---
#- name: Create user on a linux server
#  hosts: all
#  become: yes
#  gather_facts: false
#  vars:
#    - user: $JOHN
#    - password: $JOHN
#  tasks:
#      - name: Create a login user
#        user:
#          name: $JOHN
#          password: $JOHN
#          state: present 

---
- name: "Copy our landing to folder enabled"
  template:
    src: passwd.conf.j2
    dest: "/home/vagrant/passwd_user_john.conf"
